## Summary

Upgrades `backend/` to **Laravel 13** (PHP `^8.3`, `laravel/tinker` ^3, PHPUnit 12).

## Database / Turso

`turso/libsql-laravel` only supports `illuminate/database` **^11|^12**, so it is **not** included on this branch. After merge:

- **Production:** use **Laravel Cloud Serverless Postgres** or **Laravel MySQL** (attach DB + redeploy, `php artisan migrate --force`).
- **Local:** unchanged — SQLite (`DB_CONNECTION=sqlite`).

Re-add Turso when the official package supports Laravel 13.

## CI / tooling

- GitHub Actions backend job: **PHP 8.3**
- `.agents/setup`: PHP 8.3 via Sury (matches `composer.json`)
- `cache.php`: `serializable_classes` (Laravel 13 default)

## Checklist

- [ ] `php artisan test` / CI green on this PR
- [ ] Cloud env uses managed DB (not sqlite) before switching production to `main` post-merge